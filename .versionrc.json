{
    "skip": {
      "changelog": false
    },
    "commitUrlFormat": "{{host}}/{{owner}}/{{repository}}/commits/{{hash}}",
    "issueUrlFormat": "{{host}}/{{owner}}/{{repository}}/issues/{{id}}"
  }
  